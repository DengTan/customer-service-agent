---
description: Windows 开发环境启动命令
alwaysApply: true
---

# Windows 开发环境

## 启动服务

**禁止**在 Windows PowerShell 执行 `pnpm run dev`（调用 Linux Bash 脚本）。

正确方式：

```powershell
pnpm tsx watch src/server.ts
```

监听 `http://localhost:5000`。

## 环境变量

| 变量 | 默认值 |
|------|--------|
| `PORT` | 5000 |
| `HOSTNAME` | 0.0.0.0 |
