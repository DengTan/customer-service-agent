---
description: Supabase 数据库架构规范
alwaysApply: true
---

# 数据库架构规范

## 连接配置

Supabase REST API：
- `SUPABASE_URL`: `https://avmregjnnsmshwxrwjie.supabase.co`
- `SUPABASE_ANON_KEY`: JWT (role=anon)
- `SUPABASE_SERVICE_ROLE_KEY`: JWT (role=service_role)

## Schema 定义

位置：`src/storage/database/shared/schema.ts`

**表定义规范**：
- 表名：`snake_case`，复数形式
- 主键：`varchar("id", { length: 36 }).primaryKey().default(sql`gen_random_uuid()`)`
- 时间戳：`created_at: timestamp("created_at", { withTimezone: true }).defaultNow().notNull()`
- 索引命名：`{table}_{column}_idx`

## 常见字段类型

| 类型 | Drizzle 写法 | PostgreSQL |
|------|-------------|-----------|
| UUID 主键 | `varchar("id", { length: 36 })` | `varchar(36)` |
| 短字符串 | `varchar("name", { length: 100 })` | `varchar(100)` |
| 长文本 | `text("content")` | `text` |
| 时间戳 | `timestamp("created_at", { withTimezone: true })` | `timestamptz` |
| JSONB | `jsonb("config")` | `jsonb` |

## 外键关联

| 关联字段 | 指向表 |
|---------|--------|
| `shop_id` | `shops.id` |
| `product_id` | `product_details.id` |
| `parent_ticket_id` | `tickets.id` |
| `parent_bot_id` | `bot_configs.id` |
| `conversation_id` | `conversations.id` |
| `customer_id` | `customers.id` |
| `user_id` | `users.id` |

## Repository 规范

位置：`src/server/repositories/`
- 文件名：`{entity-name}-repository.ts`
- 类名：`XxxRepository`
- 导出：具名导出

## 查询规范

- 应用层使用 Supabase 客户端：`getSupabaseClient()`
- 避免 N+1：批量 `in()` 查询替代循环单条查询
- 复杂操作使用 RPC
