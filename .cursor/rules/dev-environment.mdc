---
description: Windows + Ollama 开发环境启动流程（含统一启动命令与故障排查）
alwaysApply: true
---

# 开发环境启动

## 启动顺序

**Ollama → 应用服务器**。Ollama 没起来会导致 AI 对话和知识库向量化失败。

## 1. Ollama

```powershell
# 检查状态
ollama list

# 启动（后台）
Start-Process -FilePath "ollama" -ArgumentList "serve" -NoNewWindow -PassThru

# 验证
Invoke-WebRequest -Uri "http://localhost:11434/api/tags" -UseBasicParsing
# 期望: HTTP 200
```

## 2. 应用服务器

**禁止**执行 `pnpm run dev`（调用 Linux Bash 脚本，Windows 下会失败）。

```powershell
# 推荐：项目内置 bootstrap 脚本（Next.js 16 必须在 tsx 注册前挂载 AsyncLocalStorage）
pnpm dev:win

# 等价手动命令
node --require ./scripts/asls-bootstrap.cjs --import tsx --no-warnings src/server.ts

# watch 模式（文件变更自动重启）
node --require ./scripts/asls-bootstrap.cjs --import tsx --no-warnings --watch src/server.ts
```

监听 `http://localhost:5000`。首次访问 `GET /` 应返回 HTTP 307（重定向到 `/login`）。

## 故障排查

| 问题 | 解决方案 |
|------|----------|
| `Invariant: AsyncLocalStorage accessed in runtime where it is not available` | 没有走 `asls-bootstrap.cjs`；改用 `pnpm dev:win` |
| `EADDRINUSE :::5000` | `netstat -ano \| findstr ":5000"` 找到 PID 后 `taskkill /PID <PID> /F` |
| Ollama 占用 11434 | 同上，端口换成 `:11434` |
| `Unable to acquire lock` | 删除 `.next\dev\lock` |
| Ollama 404 | `taskkill /FI "IMAGENAME eq ollama.exe" /F` 后重启 |

## 停止服务

```powershell
taskkill /FI "IMAGENAME eq node.exe" /F
taskkill /FI "IMAGENAME eq ollama.exe" /F
```

## 环境变量

| 变量 | 默认值 |
|------|--------|
| `PORT` | 5000 |
| `HOSTNAME` | 0.0.0.0 |
