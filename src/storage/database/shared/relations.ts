import { relations } from "drizzle-orm/relations";

// Note: Add specific table relations here as needed
// Example: import { tableName } from "./schema";
// export const tableNameRelations = relations(tableName, ({ one, many }) => ({ ... }));
